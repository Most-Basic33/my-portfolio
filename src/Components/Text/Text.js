import React from 'react'

const content = [
    `'The technology used to do authentication for this project was BCrypt.JS. I used my Postgres database to store the hash value of the user's password for security. The system is set up to login and register users and very important to not allow duplicate information. The application is set up with middleware for detecting privileges and allowing for a person with Admin rights to have full C-R-U-D throughout the application. Admin users can Add and Delete Properties, Persons, and update details about a house such as a price and if it is available.  `
]
export default content