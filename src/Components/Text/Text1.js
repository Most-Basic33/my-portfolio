import React from 'react'

const content = [
    `This Log combines two different authentications. You can choose to log in locally which uses BCrypt, Express, Axios, Postgres to create a user, authenticate their credentials, validate, then pull up the user's profile. Or If it's the first time you've Logged in you would Log-In with your Spotify Credentials and it would allow you access to your Spotify profile.`, ``
]
export default content